eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: monitor-service
  cloud:
    config:
      uri: http://localhost:8769
      fail-fast: true
  profiles:
      active: pro